# User specific environment
if ! [[ "$PATH" =~ "$HOME/.local/bin:$HOME/bin:" ]]
then
    export PYENV_ROOT="$HOME/.pyenv"
    PATH="$PYENV_ROOT/bin:$HOME/.local/bin:$HOME/bin:$HOME/go/bin:$PATH"
fi
export PATH
